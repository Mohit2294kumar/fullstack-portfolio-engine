{
  "name": "portfolio-mern-premium",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix api\"",
    "build": "npm run build --prefix client",
    "start": "npm run start --prefix api"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}